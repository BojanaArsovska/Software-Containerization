apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog-api.fullname" . }}
  labels:
    {{- include "blog-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
  selector:
    {{- include "blog-api.selectorLabels" . | nindent 4 }}
